type ParsedRow = {
    id: number,
    name: string,
    surname: string,
    email: string,
    age: number,
    bug: string        
}
type ParsedData = ParsedRow[]

const varb: ParsedData = {a:3, b:5}

